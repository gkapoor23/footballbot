#include "game.h"

int main() {
    // Load teams and players data from /data folder
    // Instantiate teams and players
    // Add players to the teams

    Game game(team1, team2);
    game.play();

    return 0;
}
